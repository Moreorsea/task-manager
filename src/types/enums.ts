export enum Filters {
  all = 'all',
  overdue = 'overdue',
  today = 'today',
  favorites = 'favorites',
  repeating = 'repeating',
  archive = 'archive',
}

export enum Sorts {
  default = 'default',
  up = 'up',
  down = 'down',
}

export enum API_METHODS {
  post = 'post',
  put = 'put',
}

export enum Colors {
  black = 'black',
  yellow = 'yellow',
  blue = 'blue',
  green = 'green',
  pink = 'pink',
}

export enum RepeatingDays {
  mo = 'mo',
  tu = 'tu',
  we = 'we',
  th = 'th',
  fr = 'fr',
  sa = 'sa',
  su = 'su',
}
